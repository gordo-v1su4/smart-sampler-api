version: "3.9"

# Note: This file is for local testing only.
# Production deployment uses Coolify at https://sampler.v1su4.com:5000

services:
  smart-sampler-api:
    build: .
    container_name: smart-sampler-api
    restart: unless-stopped
    ports:
      - "5000:8080"
    environment:
      - ROBYN_HOST=0.0.0.0
      - ROBYN_PORT=8080
    env_file:
      - .env
    volumes:
      - .:/app