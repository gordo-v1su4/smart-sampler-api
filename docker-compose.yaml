services:
  smart-sampler-api:
    build: .
    restart: unless-stopped
    networks:
      - coolify
    environment:
      - ROBYN_HOST=0.0.0.0
      - ROBYN_PORT=8080
    env_file:
      - .env
    volumes:
      - .:/app

networks:
  coolify:
    external: true
